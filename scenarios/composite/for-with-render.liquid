{%- comment -%}
Composite test: for loop with render
Partial rendering (scope isolated) inside loop
Requires: partials/product-card.liquid
Support: shopify >=3.0, keepsuit >=1.0
{%- endcomment -%}
{% for product in items %}
  {% render 'product-card', product: product %}
{% endfor %}
