{%- comment -%}
Unit test: filter chaining
Multiple filters in sequence
{%- endcomment -%}
{% assign product = products | first %}
{{ product.variants | where: 'available', true | sort: 'price' | map: 'title' | join: ', ' }}
