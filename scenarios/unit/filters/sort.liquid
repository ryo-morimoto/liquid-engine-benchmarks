{%- comment -%}
Unit test: sort filter
Sort array by property
{%- endcomment -%}
{% assign sorted = products | sort: 'price' %}
{% for product in sorted %}
{{ product.title }}: {{ product.price }}
{% endfor %}
