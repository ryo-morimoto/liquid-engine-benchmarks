{%- comment -%}
Unit test: simple if condition
Measures basic conditional evaluation
{%- endcomment -%}
{% assign product = products | first %}
{% if product.price > 0 %}
{{ product.title }} is available
{% else %}
{{ product.title }} is out of stock
{% endif %}
