# direnv configuration
# Automatically activates Nix development environment when entering this directory
#
# Requirements:
#   - direnv (https://direnv.net/)
#   - nix-direnv (https://github.com/nix-community/nix-direnv)
#
# Setup:
#   1. Install direnv and hook it into your shell
#   2. Run `direnv allow` in this directory
#   3. Environment will be activated automatically with caching
#
# For language-specific shells:
#   nix develop .#php   # PHP only
#   nix develop .#ruby  # Ruby only

# Use nix-direnv for fast, cached flake evaluation
if has nix_direnv_version; then
  nix_direnv_version 3.0.0
fi

use flake
